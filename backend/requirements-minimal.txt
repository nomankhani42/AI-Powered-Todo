# Minimal requirements for simplified Todo app
# Use this for development/demo with SQLite

# Core Web Framework (Required)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database ORM (Required)
sqlalchemy==2.0.23

# Validation & Settings (Required)
pydantic==2.5.0
pydantic-settings==2.1.0

# Utilities (Recommended)
python-dotenv==1.0.0

# Testing (Optional - remove if not using pytest)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
