# =============================================================================
# Fullstack AI-Powered Todo App - Environment Configuration
# =============================================================================
# This file contains configuration for both backend and frontend services.
# Copy this to .env and fill in all values before running the application.
#
# For detailed setup instructions, see:
# - backend/ENV_SETUP_GUIDE.md (comprehensive guide with step-by-step instructions)
# - QUICK_START_NEON.md (quick start guide)
#
# IMPORTANT: Never commit actual secrets - only version control .env.example
# =============================================================================

# BACKEND CONFIGURATION
# =============================================================================

# Database - Neon Serverless PostgreSQL
# Connection string format: postgresql://[user]:[password]@[neon_hostname]/[dbname]?sslmode=require
# Example: postgresql://alex:AbC123dEf@ep-cool-darkness-123456.us-east-2.aws.neon.tech/dbname?sslmode=require
#
# How to get your Neon connection string:
# 1. Go to https://console.neon.tech
# 2. Sign up for free account
# 3. Create a new project
# 4. Copy the connection string from "Connection Details"
# 5. Paste it below
#
# For detailed instructions, see backend/ENV_SETUP_GUIDE.md section 1
DATABASE_URL=postgresql://[NEON_USER]:[NEON_PASSWORD]@[NEON_HOSTNAME]/[NEON_DATABASE]?sslmode=require

# OpenRouter Configuration (for OpenAI Agents SDK)
# ============================================================================
# Required for AI-powered task management agent using Qwen models
#
# Why OpenRouter?
# - UNLIMITED FREE ACCESS with Qwen models (no rate limits!)
# - Unlike Gemini which has 1500 requests/day limit
# - Supports multiple open-source models (Qwen, Llama, etc.)
# - No payment required for free tier
#
# How to get your OpenRouter API key:
# 1. Go to https://openrouter.ai
# 2. Sign up (free account, no payment required)
# 3. Go to https://openrouter.ai/keys
# 4. Click "Create new API key"
# 5. Copy the generated API key
# 6. Paste it below as OPENROUTER_API_KEY
#
# Model Options (all free and unlimited):
# - qwen/qwen-2.5-72b-instruct: Best overall - 72B parameters, excellent quality (RECOMMENDED)
# - qwen/qwq-32b: Reasoning model - great for complex tasks
# - qwen/qwen-1.5-0.5b-chat: Lightweight - fastest
# - And many others at https://openrouter.ai/models
#
# Set via OPENROUTER_MODEL environment variable (optional, defaults to qwen-2.5-72b-instruct)
#
# IMPORTANT: Completely free tier with NO LIMITS!
# No rate limiting, no hidden costs, no payment required
# Perfect for unlimited task management operations
#
# For detailed setup instructions, see backend/ENV_SETUP_GUIDE.md section 2
OPENROUTER_API_KEY=[YOUR_OPENROUTER_API_KEY_HERE]
OPENROUTER_MODEL=qwen/qwen-2.5-72b-instruct

# JWT & Authentication
# Generate a secure secret key using one of these commands:
#   Option 1: openssl rand -hex 32
#   Option 2: python -c "import secrets; print(secrets.token_hex(32))"
#   Option 3: uv run python -c "import secrets; print(secrets.token_hex(32))"
#
# SECURITY: Never use a simple password - always use a cryptographically generated key
# SECURITY: Use different keys for development and production
# SECURITY: Rotate keys periodically (every 3-6 months)
#
# For detailed instructions, see backend/ENV_SETUP_GUIDE.md section 3
JWT_SECRET_KEY=[GENERATE_WITH_OPENSSL_RAND_HEX_32]
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
REFRESH_TOKEN_EXPIRATION_DAYS=7

# Server Configuration
DEBUG=false
HOST=0.0.0.0
PORT=8000

# CORS & Security
# Comma-separated list of allowed origins for CORS
# Update this if frontend runs on different port/domain
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8000

# Environment
# Options: development, production, staging
ENVIRONMENT=development
LOG_LEVEL=INFO

# AI Feature Configuration
# Timeout in seconds for AI API calls (prevent hanging requests)
AI_TIMEOUT_SECONDS=3
# Rate limiting: maximum number of AI requests
AI_RATE_LIMIT_CALLS=10
# Rate limiting: time window in seconds
AI_RATE_LIMIT_WINDOW=60

# FRONTEND CONFIGURATION
# =============================================================================

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Application Environment
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_NAME=Todo App

# Optional: Analytics and Monitoring
# NEXT_PUBLIC_ANALYTICS_ID=
# NEXT_PUBLIC_SENTRY_DSN=

# DOCKER CONFIGURATION
# =============================================================================

# PostgreSQL Service
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=todo_app

# Docker Build & Compose
COMPOSE_PROJECT_NAME=todo-app-fullstack
